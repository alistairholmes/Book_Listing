package com.example.alistair.booklistingapp;

public class QueryUtils {
}

// Todo The app fetches book data related to the query via an HTTP request from the Google Books API,

// Todo Use a class such as HttpUriRequest or HttpURLConnection.

// Todo The app checks whether the device is connected to the internet and responds appropriately.

// Todo The result of the request is validated to account for a bad server response or lack of server response.

// Todo The JSON response is parsed correctly, and relevant information is stored in the app.

// Todo The ListView is properly populated with the information parsed from the JSON response.

// Todo When there is no data to display, the app shows a default TextView that informs the user how to populate the list.